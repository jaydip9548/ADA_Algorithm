package Graph;

// Goal : Given a graph find the sortest path from a source to all other vertices.

// T.C  :  O(V2) using Simple Adjancency metrix and O(E logV) by using (Aj...list and Min Heap)

// Limitation : It doesnt work for -ve edge weight cycles.

// if we have vertices 0 to N  ---> Array else can use SET(Initiating) || MAP(value)
public class L4_Dijsktra_Algo {

    public static void main(String[] args) {
        
    }
}
